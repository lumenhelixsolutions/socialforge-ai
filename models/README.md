# Models

Place notes here about tested local models.

Recommended first models:

- `qwen2.5:3b` for low-resource smoke testing
- `qwen2.5:7b` or equivalent for better drafts
- A separate reviewer model if performance allows
- Any raw/uncensored/obliterated model must be used only in draft-only sandbox mode

Never store downloaded model binaries in this repository.
